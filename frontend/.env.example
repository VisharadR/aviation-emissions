# Server-side only. Keep OpenSky credentials in the Python backend.
AVIATION_API_URL=http://127.0.0.1:8000
# GitHub Pages build only: public HTTPS backend origin (never an API secret).
# NEXT_PUBLIC_LIVE_API_URL=https://your-live-backend.example
